<%= render 'refinery/admin/search_header', :url => refinery.exhibitors_admin_exhibitors_path %>
<div class='pagination_container'>
  <% if @exhibitors.any? %>
    <%= render 'exhibitors' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
