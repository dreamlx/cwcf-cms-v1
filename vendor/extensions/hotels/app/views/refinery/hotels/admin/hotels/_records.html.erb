<%= render 'refinery/admin/search_header', :url => refinery.hotels_admin_hotels_path %>
<div class='pagination_container'>
  <% if @hotels.any? %>
    <%= render 'hotels' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
