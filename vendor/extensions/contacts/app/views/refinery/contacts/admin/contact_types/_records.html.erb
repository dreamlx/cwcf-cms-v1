<%= render 'refinery/admin/search_header', :url => refinery.contacts_admin_contact_types_path %>
<div class='pagination_container'>
  <% if @contact_types.any? %>
    <%= render 'contact_types' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
