<%= form_for [refinery, :cooperations_admin, @cooperation] do |f| -%>
  <%= render '/refinery/admin/error_messages',
              :object => @cooperation,
              :include_object_name => true %>

  <div class='field'>
    <%= f.label :title -%>
    <%= f.text_field :title, :class => 'larger widest' -%>
  </div>

  <div class="field">
    <%= f.label :cooperation_avatar %>
    <%= render :partial => "/refinery/admin/image_picker", :locals => {
      :f => f,
      :field => :cooperation_avatar_id,
      :image => f.object.cooperation_avatar,
      :toggle_image_display => false
    }
    %>
  </div>

  <div class='field'>
    <%= f.label :main_url -%>
    <%= f.text_field :main_url -%>
  </div>

  <div class="field">
    <%= f.label :cooperation_type -%>
    <%= f.select(:cooperation_type_id, @cooperation_types.collect {|d| [d.name, d.id] })%>
  </div>

  <%= render '/refinery/admin/form_actions', :f => f,
             :continue_editing => false,
             :delete_title => t('delete', :scope => 'refinery.cooperations.admin.cooperations.cooperation'),
             :delete_confirmation => t('message', :scope => 'refinery.admin.delete', :title => @cooperation.title) -%>
<% end -%>
